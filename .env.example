# API server (run with `npm run dev` — starts Vite + server)
ANTHROPIC_API_KEY=
# ANTHROPIC_MODEL=claude-sonnet-4-6

# Supabase — shared card persistence (optional; falls back to in-memory store)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Production settings
# NODE_ENV=production
# PORT=8787
# ALLOWED_ORIGINS=https://resumerpg.app,https://www.resumerpg.app
# RATE_LIMIT_GENERATE=10
# RATE_LIMIT_SHARE=30
